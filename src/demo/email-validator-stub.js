// Stub: deep-email-validator usa 'dns' do Node (não existe no browser)
// Na demo, toda validação de email retorna válido
export default async function validate(email) {
  return { valid: true, reason: "demo", validators: {} };
}
