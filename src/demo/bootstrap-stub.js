/* Bootstrap JS stub — na demo, jQuery não está disponível como global antes do bundle
   O bootstrap.bundle.min.js verifica window.jQuery na inicialização e quebra.
   Os componentes que usam bootstrap JS (modais, tooltips) funcionam via CSS;
   a funcionalidade JS é progressiva e não bloqueia a demo. */
export default {};
